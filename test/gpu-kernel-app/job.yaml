apiVersion: batch/v1
kind: Job
metadata:
  name: gpu-kernel-test
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: gpu-kernel
          image: kratos-gpu-kernel-test:latest
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              nvidia.com/gpu: 1
